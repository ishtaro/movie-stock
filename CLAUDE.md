# movie-stock

観た映画を記録し、アチーブメント解錠で継続を促す Web サービス。

## 概要

- **何を作るか**: 鑑賞した映画をコレクションに登録し、鑑賞日・評価・感想・鑑賞方法を保存できるマルチユーザー向け Web アプリ。記録の蓄積に応じてアチーブメント（本数・ジャンル・鑑賞方法など）が解錠されるゲーム的要素を持つ。
- **解決する課題**: 鑑賞体験が記録に残らず振り返れないこと（R-1）、記録行為に楽しみがなく続かないこと（R-2）。詳細は SPEC.md 第1部。
- **対象**: 不特定多数の一般ユーザー。ただし SNS 的な公開・交流機能は**恒久的に対象外**（各ユーザーのプライベートな記録ツール）。
- **利用環境**: スマホ・PC 両対応のレスポンシブ Web（375px〜1920px）。

## ドキュメント構成

| ファイル | 内容 |
|----------|------|
| SPEC.md | 要求定義書（課題・成功指標・スコープ）+ 要件定義書（機能要件 F-01〜F-10・画面・データ・非機能・用語集・未決事項） |
| TASKS.md | フェーズ 0〜5 のタスク分割・完了条件・技術スタック・運用ルール |
| DESIGN.md | デザイン設計書（デザイン原則 P-1〜P-5・トークン・画面別レイアウト S-1〜S-9・解錠演出仕様） |
| SKILL.md | 要求定義・要件定義に用いた思考プロトコル（要件変更時はこれに沿って更新する） |

## 技術スタック

Next.js（App Router）+ TypeScript + Tailwind CSS / Supabase（Auth + PostgreSQL + RLS）/ Vercel（Hobby）/ TMDb API（作品情報、language=ja-JP）。無料枠中心で運用する制約がある。

## 開発時の必須ルール

- **RLS 必須**: ユーザーデータを持つ全テーブルに行レベルセキュリティを適用し、本人以外のアクセスを DB 層で遮断する。
- **TMDb API キーはサーバー側のみ**: クライアントに露出させず、検索はサーバー経由でプロキシする。TMDb の帰属表示を常時掲載する。
- **アチーブメントはデータ駆動**: 定義はテーブル/シードデータで持ち、追加時にロジック変更を不要にする。一度解錠したものは剥奪しない。
- **1ユーザー1作品1記録**: viewing_records は (user_id, movie_id) でユニーク。再鑑賞は既存記録の編集で扱う（複数回記録はフェーズ5で再設計）。
- **仕様変更時**: SPEC.md の該当箇所と未決事項リストを更新してから実装する。【仮】マークの付いた値は確定したら本文に反映する。

## 用語（詳細は SPEC.md 第2部6章）

作品（TMDb 上の映画・全ユーザー共有）/ 鑑賞記録（ユーザー×作品の記録）/ コレクション（自分の鑑賞記録の集合）/ アチーブメント（条件達成で解錠される称号）。この4語に統一し、揺れ（ライブラリ・バッジ等）を使わない。
