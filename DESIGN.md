# movie-stock デザイン設計書

SPEC.md 第2部の画面一覧（S-1〜S-9）を対象に、UI デザインを定義する。
SKILL.md の原則に従い、**画面は要件の投影**として設計し（機能 ID F-xx への対応を明記）、
形容詞は検証可能な数値・判定手順に変換する。**【仮】** は仮置きであり変更されうる。

## 1. デザイン原則（要求からの導出）

| # | 原則 | 導出元 | 検証可能な判定基準 |
|---|------|--------|--------------------|
| P-1 | 記録は1分以内 | R-3（観終わった直後にスマホで記録） | ログイン済み状態から「検索タブ → 検索語入力 → 候補タップ → 保存」の**4操作**で記録が完了する。中間画面を増やす変更は本原則違反 |
| P-2 | ポスターが主役 | R-1（振り返る楽しさ） | コレクション一覧の視覚面積の 60% 以上をポスター画像が占める。一覧にテキストメタ情報（あらすじ等）を並置しない |
| P-3 | 達成が光る | R-2（継続動機） | アチーブメント解錠時、他のどの UI イベントとも異なる専用演出（§7）が発火する。解錠済みと未解錠は色彩（ゴールド/グレー）だけでなく形状（バッジ/シルエット）でも区別される |
| P-4 | 片手で完結 | R-3（スマホ利用） | 主要操作（タブ切替・保存・戻る）のタップ対象が画面下半分に配置され、タップ領域は 44×44px 以上 |
| P-5 | 暗闇で読める | ビジュアルトーン決定（ダークシネマ調） | 本文テキストのコントラスト比 4.5:1 以上、補助テキスト 3:1 以上（WCAG AA 相当の値のみ採用。基準全体への準拠は SPEC 非機能要件どおり対象外） |

## 2. デザイントークン

実装時は Tailwind の theme 拡張として定義し、ハードコードした色値の使用を禁止する。

### 2-1. カラー（ダークシネマ調・ダークテーマのみ提供）

ライトテーマは提供しない（やらないこと。切替機能はフェーズ5でも未計画）。

| トークン | 値 | 用途 |
|----------|-----|------|
| `bg-base` | `#0B0E14` | 画面背景（劇場の暗闇） |
| `bg-surface` | `#141926` | カード・フォーム・ナビの面 |
| `bg-elevated` | `#1C2333` | モーダル・トースト・ホバー面 |
| `border-subtle` | `#2A3247` | 区切り線・カード枠 |
| `text-primary` | `#EDEFF5` | 見出し・本文（bg-base 上でコントラスト比 約16:1） |
| `text-secondary` | `#9AA3B8` | 補助情報・ラベル（同 約7:1） |
| `text-muted` | `#5C6579` | プレースホルダー・無効状態（同 約3.4:1、補助のみ） |
| `accent-gold` | `#E3B341` | 主アクセント: 評価の星・解錠バッジ・主ボタン・フォーカスリング |
| `accent-gold-deep` | `#B8860B` | ゴールドのグラデーション終端・押下状態 |
| `error` | `#E5534B` | エラー・削除操作 |
| `success` | `#3FB950` | 保存成功の確認表示 |

- ゴールドはアチーブメント・評価・主 CTA の3用途に限定する。装飾に乱用すると P-3（達成が光る）の希少性が失われる。
- 主ボタン上のテキストは `#0B0E14`（ゴールド地に暗色文字。コントラスト比 約9:1）。

### 2-2. タイポグラフィ

| トークン | 値 | 用途 |
|----------|-----|------|
| フォント | `"Noto Sans JP", system-ui, sans-serif`（Next.js の font 最適化で自己ホスト） | 全 UI |
| `text-xl` | 20px / bold | 画面タイトル |
| `text-lg` | 17px / bold | カード見出し・作品タイトル（詳細） |
| `text-base` | 15px / regular / 行間1.8 | 本文・感想 |
| `text-sm` | 13px / regular | 補助情報（鑑賞日・ジャンル） |
| `text-xs` | 11px / regular | バッジ・タブラベル |

感想本文（読む対象）は行間 1.8、それ以外の UI テキストは 1.4 **【仮】**。

### 2-3. 形状・余白・動き

| 項目 | 値 |
|------|-----|
| 角丸 | ポスター 8px / カード・入力欄 12px / ボタン 10px / バッジ円形 |
| 余白スケール | 4 / 8 / 12 / 16 / 24 / 32px の6段のみ使用 |
| ポスター縦横比 | 2:3 固定（TMDb w342 を使用、`object-fit: cover`） |
| 影 | ダーク背景のため影は使わず、面の明度差（surface/elevated）で階層を表現 |
| トランジション | 150ms ease-out（ホバー・タブ切替）。`prefers-reduced-motion` 時は解錠演出含め全アニメーションを無効化し即時表示 |

## 3. ナビゲーション構造

### モバイル（〜767px）: 下部タブバー（P-4）

| タブ | アイコン **【仮】**（lucide） | 画面 |
|------|------------------------------|------|
| コレクション | `film` | S-3 |
| さがす | `search` | S-4 → S-5 |
| アチーブメント | `trophy` | S-7 |
| 設定 | `user` | S-8 |

- タブバー高さ 56px + セーフエリア。現在タブはゴールド、非選択は `text-secondary`。
- 未読の解錠があるとき、アチーブメントタブにゴールドのドット（8px）を表示し、S-7 を開くと消える。

### デスクトップ（768px〜）: 上部ヘッダー

左にロゴ（未決-7 のサービス名）、右に同4項目を水平配置。コンテンツ最大幅 1200px 中央寄せ。

## 4. レスポンシブ規則

| ブレークポイント | コレクショングリッド列数 | 備考 |
|------------------|--------------------------|------|
| 375〜639px | 3列（gap 12px） | 基準デバイス。ポスター幅 約109px |
| 640〜1023px | 4列 | |
| 1024px〜 | 6列（最大幅 1200px） | |

- 320px 未満は動作保証外（SPEC 互換性: 375px〜1920px）。
- フォーム系画面（S-2, S-5）は全幅で最大 480px に制限し中央寄せ。

## 5. 画面別レイアウト仕様

### S-1 ランディング（F-01 導線・F-09）

- 上から: ロゴ / キャッチコピー1行 / ポスターグリッドのイメージビジュアル / 「無料ではじめる」（主ボタン・ゴールド）/ 「ログイン」（テキストリンク）/ フッター（規約・ポリシー・TMDb 帰属表示）。
- ファーストビュー（375×667px）内に主ボタンが収まること。

### S-2 会員登録 / ログイン / パスワード再設定（F-01）

- 1画面1フォーム。項目: メール・パスワードの2つのみ。
- エラーは該当フィールド直下に `error` 色 13px で表示（画面上部への集約表示はしない）。
- 送信中はボタンをスピナー付き無効化し二重送信を防ぐ。

### S-3 コレクション一覧（F-05）— ホーム

- 構成: 画面タイトル「コレクション」+ 記録本数（例「42本」）/ 並び替え・絞り込み行 〔Should〕 / ポスターグリッド。
- グリッドセル: ポスターのみ（P-2）。評価済みは左下に星と数値のミニバッジ（`bg-elevated` 半透明地）。タイトルテキストは表示しない（ポスターで識別。取得不可時のみ §6 プレースホルダー）。
- ページング: 20件ずつの無限スクロール **【仮】**。読込中はポスター形のスケルトンを3枚表示。
- 空状態: 中央に `film` アイコン + 「最初の1本を記録しよう」+ 検索画面への主ボタン（SPEC F-05 例外系）。

### S-4 作品検索（F-03）

- 画面遷移直後に検索入力へ自動フォーカス（モバイルはキーボードを即時表示）。
- 入力から 400ms のデバウンス後に検索実行 **【仮】**（明示ボタン不要。P-1 の操作数に数えない）。
- 結果行: ポスターサムネイル（幅56px）+ タイトル + 公開年。行全体がタップ対象（高さ 72px ≥ 44px）。
- 0件: 「見つかりません。英題や別の表記も試してください」（SPEC F-03 例外系の文言をそのまま使用）。
- TMDb 障害: エラーメッセージ + 「再試行」ボタン。入力語は保持する。

### S-5 記録作成・編集フォーム（F-04, F-06）

- 上部に選択作品の確認（サムネイル + タイトル + 公開年。変更は検索へ戻る）。
- 項目順: 鑑賞日（既定=当日、ネイティブ date picker）→ 鑑賞方法（5択のセグメント/チップ、横スクロール可）→ 評価（星5つ、タップで設定・再タップで解除=未評価）→ 感想（textarea、残り文字数を 1,800字超から表示）。
- 保存ボタンは画面下部に固定（P-4）。必須（鑑賞日・鑑賞方法）が揃うまで無効。
- 失敗時: 入力値を保持したまま上部にエラーバナー（SPEC F-04 例外系）。
- 重複作品選択時: フォームではなく「既に記録済みです」ダイアログ →「記録を開く」で S-6 へ（SPEC F-04 例外系）。

### S-6 記録詳細（F-05, F-06）

- 上部: ポスター（幅40%）+ タイトル・公開年・ジャンルチップ・鑑賞方法バッジ。
- 中央: 鑑賞日・星評価・感想全文。
- 下部: 「編集」（副ボタン）/「削除」（`error` 色テキストボタン → 確認ダイアログ。確認の主ボタンはゴールドではなく `error`）。

### S-7 アチーブメント一覧（F-08）

- 上部サマリー: 「解錠 12 / 34」+ ゴールドの進捗バー。
- 分類（本数 / ジャンル / 鑑賞方法 / 時期）ごとのセクションに 3列グリッド **【仮】**。
- 解錠済みカード: ゴールドの円形バッジ + 名称 + 解錠日（13px）。
- 未解錠カード: グレーのシルエットバッジ（グレースケール、P-3 の形状差）+ 名称 + 条件文 + 進捗バー（例 7/10）〔Should〕。
- 隠しアチーブメントは設けない **【仮】**（全条件を最初から可視化。未決-D1）。

### S-8 設定（F-02）

- リスト形式: メールアドレス表示 / ログアウト / 利用規約 / プライバシーポリシー / 退会。
- 退会は `error` 色。確認ダイアログでは「削除される内容（記録◯本・アチーブメント◯個）」を数値で示し、「退会する」の入力欄タイプ確認は行わない **【仮】**（過剰防御より P-1 の軽さを優先。誤操作はダイアログ2段で防ぐ）。

### S-9 規約・ポリシー（F-09）

- 静的ページ。本文最大幅 640px、`text-base` 行間 1.8。フッターに TMDb ロゴ + 帰属文言（全画面共通フッターにも常設）。

## 6. 共通コンポーネント

| コンポーネント | 仕様 |
|----------------|------|
| ポスター | 2:3、角丸8px。読込中はスケルトン（`bg-surface` のパルス）。取得不可時は `bg-surface` 地に作品タイトルを中央表示するプレースホルダー（SPEC F-05 例外系） |
| 主ボタン | ゴールド地・暗色文字・高さ48px・角丸10px。1画面に1つまで |
| 副ボタン | `border-subtle` 枠・`text-primary`・透明地 |
| 星評価 | 星1つ32px（入力時）/16px（表示時）。ゴールド。未評価は「未評価」テキスト（星0との混同を防ぐ） |
| チップ | ジャンル・鑑賞方法。`bg-elevated` 地・13px・角丸バッジ |
| トースト | 画面下部（タブバーの上）。成功=`success` アイコン、失敗=`error`。4秒で自動消滅 **【仮】** |
| ダイアログ | `bg-elevated`、幅 min(90vw, 400px)。破壊的操作の確認ボタンは `error` 色 |
| フォーカスリング | 全操作要素に `accent-gold` 2px のリング（キーボード操作時、P-5） |

## 7. アチーブメント解錠演出（F-07・P-3）

1. 記録保存の成功トースト表示後 500ms、解錠トーストを重ねて表示: ゴールドのバッジが回転しながら拡大（400ms）→ 名称「『映画好き』を解錠！」。
2. 複数同時解錠時は 1.5秒間隔で順次表示（同時に重ねない）。
3. トーストのタップで S-7 の該当アチーブメントへ遷移。無操作なら 5秒で消滅。
4. `prefers-reduced-motion` 時はアニメーションなしで同内容を静的表示。
5. 演出はゴールド + バッジ形状のみで構成し、効果音は使わない（公共の場・映画館直後の利用を想定）。

判定基準: 通常トースト（§6）と解錠トーストを並べたスクリーンショットで、被験者が説明なしにどちらが「特別」か識別できること **【仮】**。

## 8. デザイン上の未決事項

| # | 事項 | 現在の仮置き | 決定期限 |
|---|------|--------------|----------|
| 未決-D1 | 隠しアチーブメント（条件非公開）の有無 | なし（全件可視） | フェーズ3着手前（未決-1 と同時に決定） |
| 未決-D2 | ロゴ・ワードマーク（未決-7 のサービス名に依存） | テキストロゴ「MOVIE STOCK」 | リリース前 |
| 未決-D3 | アイコンセット | lucide | フェーズ2着手前 |
| 未決-D4 | 一覧の無限スクロール vs ページャ（記録1,000本超の性能） | 無限スクロール、実測で見直し | フェーズ4の性能確認時 |
| 未決-D5 | 解錠演出の強度（§7 で十分か、全画面演出にするか） | トースト演出 | フェーズ3のプロトタイプで判断 |

## 9. モックアップ

主要画面（S-3・S-5・S-7）とデザイントークンのビジュアルモックアップを Artifact として作成済み（デザイン検討用。実装の正はあくまで本書とトークン定義）。
