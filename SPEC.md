# movie-stock 要求定義書・要件定義書

本書は SKILL.md（Fable-Grade Requirements）のプロトコルに沿って作成した。
**【仮】** の付いた記述は仮説・仮置きの値であり、依頼者の確認により変更されうる。

---

# 第1部 要求定義書（問題空間）

## 1. 背景と課題

| ID | 誰が | どの場面で | 何に困っているか | 解決されると何が変わるか |
|----|------|-----------|------------------|--------------------------|
| R-1 | 映画を日常的に観る人 | 映画を観終わった後 | いつ・何を観て・どう感じたかが記録に残らず、後から振り返れない。時間が経つと感想も鑑賞日も思い出せない | 鑑賞履歴と感想がいつでも振り返れる状態になる |
| R-2 | 同上 | 記録を続けようとする場面 | 記録行為そのものに楽しみがなく、手帳やメモアプリでは続かない **【仮】** | アチーブメント解錠という報酬により、記録の継続率が上がる |
| R-3 | 同上 | 映画館や外出先で観終わった直後 | PCでしか使えないツールだと、記憶が新しいうちに記録できない **【仮】** | スマホからその場で記録できる |

- 依頼文の「アプリを作って」は解決策の形である。裏の課題は R-1（振り返れない）と R-2（続かない）であり、アチーブメントは R-2 に対する解決策として妥当と判断した。
- ヒアリングにより「不特定多数向けサービス」であることを確認済み。ただし公開・交流機能は今後も予定なしのため、**各ユーザーのプライベートな記録ツールを多人数に提供する**サービスと定義する。

## 2. 目的と成功指標

**目的**: 映画の鑑賞体験（作品・日付・感想・評価）を蓄積し、ゲーム的な達成感によって記録の継続を促す Web サービスを提供する。

**成功指標**（すべて 【仮】。リリース後に実測して見直す）:

1. リリース後3ヶ月で登録ユーザー 100 人
2. 登録ユーザーのうち 30% が月4本以上の鑑賞記録を登録している（継続率の代理指標）
3. 登録ユーザーのうち 50% がアチーブメントを1つ以上解錠している（ゲーム要素が機能している判定）

## 3. ステークホルダー

| 種別 | 内容 |
|------|------|
| エンドユーザー | 一般ユーザー（不特定多数）。ロールは1種のみで管理者ロールとの2種構成 |
| 運用者 | 開発者本人（1名）。問い合わせ対応・監視・障害対応を兼務 |
| 意思決定者 | 開発者本人 |
| 外部システム | TMDb API（作品情報）、Supabase（認証・DB）、Vercel（ホスティング）**【仮】**（技術選定は第2部参照） |
| 規制・法務 | 個人情報保護法（メールアドレスを扱う）、TMDb 利用規約（帰属表示義務）、利用規約・プライバシーポリシーの掲載 |

## 4. スコープ / やらないこと

### スコープ（初期リリース = MVP）

- 会員登録・ログイン（マルチユーザー）
- TMDb API による作品検索・作品情報の自動取得
- 鑑賞記録の作成・閲覧・編集・削除（鑑賞日・評価・感想・鑑賞方法）
- コレクション（自分の鑑賞記録一覧）表示
- アチーブメントの自動判定・解錠・一覧表示
- スマホ・PC 両対応のレスポンシブ UI

### やらないことリスト

| 項目 | 扱い |
|------|------|
| 感想・記録の公開、フォロー、いいね等の SNS 機能 | **恒久的に対象外**（ヒアリングで確認済み） |
| 同一映画の複数回鑑賞の個別記録 | 初期リリース対象外（1ユーザー1作品1記録。再鑑賞は既存記録を編集）。将来検討 |
| 観たいリスト（ウィッシュリスト） | 初期リリース対象外。フェーズ2候補 |
| 統計ダッシュボード（月別本数・ジャンル分布のグラフ） | 初期リリース対象外。フェーズ2候補 |
| スマホネイティブアプリ | 対象外（レスポンシブ Web で代替） |
| TV ドラマ・アニメシリーズ等、映画以外の記録 | 対象外（映画のみ） |
| 多言語対応 | 対象外（日本語 UI のみ） |
| 作品情報の手動登録 | 初期リリース対象外 **【仮】**（TMDb に存在しない作品は記録できない。問題になれば追加） |

## 5. 制約

| 項目 | 内容 |
|------|------|
| 予算 | 無料枠中心で運用（ヒアリングで確認済み）。ユーザー増加時に有料プランを検討 |
| 納期 | 指定なし **【仮】**。TASKS.md のフェーズ順に段階リリース |
| 体制 | 開発・運用とも1名 |
| 技術 | 指定なし。無料枠制約から第2部で選定 |
| 外部 | TMDb API の無料利用条件（帰属表示、レート制限 約50リクエスト/秒）に従う |

---

# 第2部 要件定義書（解決空間）

## 1. 機能要件

優先度は MoSCoW。**MVP ライン = Must のみ**（成功指標1〜3 は Must だけで達成可能と判断）。

### F-01 会員登録・ログイン 〔Must〕 ← R-1, R-3

> ユーザーとして、自分専用の記録を持ちたい。なぜなら他人に見られず、どの端末からも自分の記録にアクセスしたいから。

- ユーザーがメールアドレスとパスワードを登録した場合、システムは確認メールを送信し、メール内リンクの踏破をもってアカウントを有効化する。
- 有効化済みユーザーが正しい認証情報でログインした場合、システムはコレクション一覧画面を表示する。

受け入れ基準:
- [ ] 登録 → 確認メール → ログイン → 一覧表示 の一連の操作が完了できる
- [ ] ユーザー A でログインした状態でユーザー B の記録・アチーブメントは一切取得できない（API 直叩きを含む）

例外系:
- 登録済みメールアドレスで再登録した場合、システムは「登録済み」を示すエラーを表示する（有効なアカウントの存在を第三者に開示しない文言にするかは未決 → 未決-6）。
- 認証失敗が5回連続した場合、システムは10分間そのアカウントのログインを拒否する **【仮】**。
- 未ログインで要ログイン画面の URL に直接アクセスした場合、システムはログイン画面へリダイレクトする。
- パスワード忘れの場合、システムはメール経由の再設定フローを提供する。

### F-02 ログアウト・退会 〔Must〕 ← R-1

- ユーザーが退会を実行した場合、システムは確認ダイアログを経て、当該ユーザーのアカウント・鑑賞記録・アチーブメント解錠履歴を即時削除する **【仮】**（猶予期間なし → 未決-3）。

例外系:
- 退会処理が途中で失敗した場合、システムは全削除をロールバックし、アカウントを削除前の状態に保つ（削除は単一トランザクションで行う）。

### F-03 映画検索（TMDb 連携） 〔Must〕 ← R-1

> ユーザーとして、タイトルの一部を入れるだけで作品を特定したい。なぜならポスターやジャンルを手で入力するのは面倒で続かないから。

- ユーザーが検索語を入力した場合、システムは TMDb API（日本語設定）で検索し、タイトル・公開年・ポスター画像を最大20件表示する。
- ユーザーが検索結果から作品を選択した場合、システムは作品情報（TMDb ID・タイトル・ポスター・ジャンル・公開日）を自作品キャッシュに保存し、記録作成フォームへ遷移する。

受け入れ基準:
- [ ] 「君の名は」で検索して該当作品がポスター付きで表示され、選択すると記録作成フォームにタイトルが自動入力されている

例外系:
- 検索結果が0件の場合、システムは「見つからない場合は英題や別表記を試してください」の案内を表示する。
- TMDb API がエラーまたは3秒以内に応答しない場合、システムは「作品情報の取得に失敗しました。時間をおいて再試行してください」を表示する（記録済みデータの閲覧は影響を受けない）。
- 空文字・空白のみで検索した場合、システムは API を呼ばずに入力を促す。

### F-04 鑑賞記録の作成 〔Must〕 ← R-1, R-3

> ユーザーとして、観終わった直後にスマホから1分で記録したい。なぜなら後回しにすると感想を忘れるから。

- ユーザーが作品を選択し鑑賞日を入力して保存した場合、システムは鑑賞記録を作成しコレクションに追加する。
- 入力項目: 鑑賞日（必須・当日が既定値）、評価（任意・1〜5の整数 **【仮】** → 未決-2）、感想（任意・最大2,000文字 **【仮】**）、鑑賞方法（必須・「映画館 / 配信 / DVD・Blu-ray / TV放送 / その他」から選択 **【仮】**）。

受け入れ基準:
- [ ] 必須2項目（鑑賞日・鑑賞方法）のみで保存できる
- [ ] 保存直後にコレクション一覧に反映され、アチーブメント判定（F-07）が実行される

例外系:
- 鑑賞日に未来の日付が指定された場合、システムはエラーを表示し保存しない。
- 既に記録済みの作品を再度記録しようとした場合、システムは既存記録の編集画面へ誘導する（1ユーザー1作品1記録。DB のユニーク制約でも保証する）。
- 感想が2,000文字を超える場合、システムは超過を示し保存しない。
- 保存処理が通信断等で失敗した場合、システムは入力内容を画面に保持したままエラーを表示する（入力の消失を防ぐ）。

### F-05 コレクション一覧・記録詳細 〔Must〕 ← R-1

- ユーザーがコレクション一覧を開いた場合、システムは自分の鑑賞記録をポスター画像のグリッドで鑑賞日の降順に表示する。
- 一覧は無限スクロールまたはページングで20件ずつ読み込む **【仮】**。
- ユーザーが記録を選択した場合、システムは作品情報・鑑賞日・評価・感想・鑑賞方法を表示する。
- 並び替え（鑑賞日昇順/降順・評価順）と、鑑賞方法による絞り込みを提供する 〔Should〕。

例外系:
- 記録が0件の場合、システムは検索画面への導線付きの空状態メッセージを表示する。
- ポスター画像が取得できない作品の場合、システムはタイトル入りのプレースホルダーを表示する。

### F-06 記録の編集・削除 〔Must〕 ← R-1

- ユーザーが自分の記録を編集・保存した場合、システムは記録を更新する。削除の場合、確認ダイアログを経て削除する。

例外系:
- 他ユーザーの記録 ID を指定して編集・削除 API を呼んだ場合、システムは 403 相当で拒否する（DB の行レベルセキュリティでも保証する）。
- 同一ユーザーが複数タブで同じ記録を同時編集した場合、後勝ち（last-write-wins）とする **【仮】**。
- 削除済みの記録への操作は 404 相当で拒否する。

### F-07 アチーブメント判定・解錠 〔Must〕 ← R-2

> ユーザーとして、記録を重ねると称号が解錠されてほしい。なぜなら達成感が次の記録の動機になるから。

- 鑑賞記録が作成・更新された場合、システムは当該ユーザーの未解錠アチーブメント全件について条件を判定し、条件を満たしたものを解錠日時付きで解錠する。
- 解錠が発生した場合、システムは画面上に解錠演出（トースト等）を表示する。
- **一度解錠したアチーブメントは、その後の記録削除で条件を下回っても剥奪しない**（確定仕様とする。剥奪はユーザー体験を損なうため）。
- アチーブメント定義はコードではなくデータ（定義テーブル/定義ファイル）として持ち、追加時にロジック変更を要しない構造にする。

初期ラインナップ **【仮】**（叩き台。最終確定は未決-1）:

| 分類 | 例 |
|------|----|
| 本数 | 1本（はじめの一歩）/ 10本 / 50本 / 100本 |
| ジャンル | 同一ジャンル5本（ジャンルごと）/ 5ジャンル制覇 / 全ジャンル制覇 |
| 鑑賞方法 | 映画館で1本 / 映画館で10本 / 配信で10本 |
| 時期 | 同一月内に5本 / 公開年が自分の記録内で50年以上離れた2作品 |

受け入れ基準:
- [ ] 10本目の記録を保存した瞬間に「10本」アチーブメントが解錠され、演出が表示される
- [ ] 記録を削除して9本になっても「10本」は解錠済みのまま残る

例外系:
- 判定処理が失敗した場合でも、鑑賞記録の保存自体は成功させる（判定は記録保存とは独立に失敗しうる。次回の記録作成時に再判定されるため取りこぼしは自己修復される）。
- 同時に複数の記録が保存された場合でも、同一アチーブメントが二重に解錠されない（ユニーク制約で保証）。

### F-08 アチーブメント一覧 〔Must〕 ← R-2

- ユーザーがアチーブメント画面を開いた場合、システムは全アチーブメントを「解錠済み（解錠日付き）/ 未解錠」で区別して表示する。
- 未解錠のものは達成条件と進捗（例: 7/10本）を表示する 〔Should〕。

例外系:
- 進捗計算が失敗した場合、進捗バーのみ非表示にし、一覧表示は維持する。

### F-09 利用規約・プライバシーポリシー・TMDb 帰属表示 〔Must〕 ← 法規制

- システムは利用規約・プライバシーポリシーを未ログインでも閲覧可能な静的ページとして提供する。
- システムは TMDb のロゴと規定の帰属文言（「This product uses the TMDB API but is not endorsed or certified by TMDB」相当）をフッター等に常時表示する。

### F-10 管理者機能 〔Could〕

- 管理者がユーザー数・記録数の集計を閲覧できる。初期リリースでは Supabase ダッシュボードで代替する **【仮】**。

## 2. 画面一覧・遷移

| # | 画面 | 主対応機能 | 認証 |
|---|------|-----------|------|
| S-1 | ランディング（サービス紹介 + ログイン/登録導線） | F-01 | 不要 |
| S-2 | 会員登録 / ログイン / パスワード再設定 | F-01 | 不要 |
| S-3 | コレクション一覧（ホーム） | F-05 | 必要 |
| S-4 | 作品検索 | F-03 | 必要 |
| S-5 | 記録作成・編集フォーム | F-04, F-06 | 必要 |
| S-6 | 記録詳細 | F-05, F-06 | 必要 |
| S-7 | アチーブメント一覧 | F-08 | 必要 |
| S-8 | 設定（ログアウト・退会） | F-02 | 必要 |
| S-9 | 利用規約 / プライバシーポリシー | F-09 | 不要 |

主遷移: S-1 → S-2 → S-3（ログイン後ホーム）。S-3 → S-4 → S-5（検索から記録）。S-3 → S-6 → S-5（既存記録の編集）。S-3 ⇄ S-7（タブ/ナビ切替）。全画面はスマホ幅（375px）〜PC幅で崩れなく表示される。

## 3. データ要件

主要エンティティ（列は代表のみ）:

| エンティティ | 内容 | 1年後の件数見込み **【仮】** |
|--------------|------|------------------------------|
| users | Supabase Auth 管理。メールアドレス、パスワードハッシュ | 1,000 |
| movies | TMDb 作品キャッシュ（全ユーザー共有）。tmdb_id(unique), タイトル, ポスターパス, ジャンル配列, 公開日 | 10,000 |
| viewing_records | 鑑賞記録。user_id, movie_id, 鑑賞日, 評価, 感想, 鑑賞方法。(user_id, movie_id) にユニーク制約 | 30,000（1,000人 × 年30本） |
| achievements | アチーブメント定義。コード, 名称, 説明, 判定種別, 閾値, アイコン | 30〜50 |
| user_achievements | 解錠履歴。(user_id, achievement_id) にユニーク制約, 解錠日時 | 15,000 |

- 個人情報はメールアドレスのみ。感想はプライベートな自由記述として個人情報に準じて扱う。
- 既存データの移行: なし（新規サービス）。
- 全ユーザーデータのテーブルに行レベルセキュリティ（RLS）を適用し、本人以外のアクセスを DB 層で遮断する。

## 4. 外部連携要件

| 連携先 | 用途 | 要件 |
|--------|------|------|
| TMDb API | 作品検索・作品情報・ポスター画像 | API キーはサーバー側のみで保持しクライアントに露出しない。検索リクエストはサーバー経由でプロキシする。language=ja-JP を既定とする。帰属表示義務（F-09）。障害時は F-03 例外系の通り劣化運転 |
| Supabase | 認証・PostgreSQL・RLS | 無料枠（500MB DB）。制限超過が近づいたら有料化判断（未決-5） |
| Vercel | ホスティング・CI/CD | 無料枠（Hobby）。商用利用制限に注意（未決-5） |

## 5. 非機能要件

| 分類 | 要件（**【仮】** = 相場からの仮置き） |
|------|----------------------------------------|
| 性能 | 想定: 登録1,000人・DAU 50・同時接続 10 **【仮】**。コレクション一覧の初回表示 2秒以内、記録保存 1秒以内、作品検索 3秒以内（TMDb 応答含む）**【仮】** |
| 可用性 | ベストエフォート（無料枠のため SLA なし）。目標稼働率 99%/月 **【仮】**。RTO 24時間・RPO 24時間 **【仮】** |
| セキュリティ | 認証: メール+パスワード（Supabase Auth、パスワード8文字以上 **【仮】**）。Google OAuth はフェーズ2 〔Should〕。ロール: 一般/管理者の2種。全通信 HTTPS。保存時暗号化は Supabase 既定に従う。個人情報＝メールアドレスのみ、退会時即時削除 **【仮】** |
| 運用・保守 | 監視: Vercel/Supabase 標準ダッシュボード + Sentry 無料枠でエラー通知 **【仮】**。バックアップ: Supabase 無料枠は自動バックアップなしのため、GitHub Actions で週1回 pg_dump を取得 **【仮】**（→未決-4）。ログ保持: プラットフォーム既定（Vercel 無料枠は1日）。問い合わせ窓口: 連絡用メールアドレスをフッターに記載 **【仮】** |
| 拡張性 | 上記データ件数見込みに対し無料枠 500MB で1年は収容可能と試算 **【仮】**。アチーブメントはデータ駆動で追加可能にする（F-07） |
| 互換性 | 対応: Chrome / Edge / Firefox / Safari の最新2バージョン、iOS Safari 16+、Android Chrome **【仮】**。画面幅 375px〜1920px。IE 非対応 |
| 法規制 | 個人情報保護法: プライバシーポリシー掲載、利用目的の明示、退会時削除。TMDb 利用規約: 帰属表示。アクセシビリティ: 特定基準への準拠は目標としない（キーボード操作・コントラストの基本配慮のみ）**【仮】**。特商法: 無料サービスのため対象外 |

## 6. 用語集

| 用語 | 定義 | 統一前の揺れ |
|------|------|--------------|
| 作品 | TMDb 上の映画1件。全ユーザーで共有されるマスタ情報 | 映画・タイトル |
| 鑑賞記録 | あるユーザーがある作品を観た事実の記録（鑑賞日・評価・感想・鑑賞方法）。1ユーザー1作品につき1件 | 記録・ログ |
| コレクション | あるユーザーの鑑賞記録の集合。依頼文の「コレクションに入れる」＝鑑賞記録の作成 | ライブラリ |
| 鑑賞方法 | 映画館 / 配信 / DVD・Blu-ray / TV放送 / その他 の5区分 **【仮】** | 見た方法 |
| アチーブメント | 条件達成で解錠される称号。定義（全ユーザー共通）と解錠履歴（ユーザーごと）から成る | 実績・バッジ |
| 解錠 | ユーザーがアチーブメントの条件を満たし、獲得済みになること。不可逆 | 解放・獲得 |

## 7. 未決事項リスト

| # | 事項 | 現在の仮置き | 決定期限の目安 |
|---|------|--------------|----------------|
| 未決-1 | アチーブメントの最終ラインナップ（名称・条件・数） | F-07 の叩き台（約30個） | フェーズ3着手前 |
| 未決-2 | 評価スケール（1〜5整数 / 0.5刻み / 10点制） | 1〜5 の整数 | フェーズ2着手前 |
| 未決-3 | 退会時のデータ削除猶予（即時 / 30日猶予） | 即時削除 | フェーズ4着手前 |
| 未決-4 | 無料枠でのバックアップ方式 | GitHub Actions で週1 pg_dump | フェーズ4着手前 |
| 未決-5 | Vercel Hobby の商用利用制限・Supabase 無料枠超過時の移行判断 | 無料枠で開始、閾値到達時に判断 | ユーザー増加時 |
| 未決-6 | 登録済みメールでの再登録時に存在を開示するか（セキュリティと UX のトレードオフ） | 開示しない汎用文言 | フェーズ1着手前 |
| 未決-7 | サービス名・ドメイン取得 | リポジトリ名 movie-stock を仮称に | リリース前 |
| 未決-8 | 成功指標（第1部2章）の数値の妥当性 | 仮値のまま計測開始 | リリース3ヶ月後に見直し |
