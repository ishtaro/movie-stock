# movie-stock 開発タスク・今後の方針

SPEC.md の要件を実現するためのフェーズ分割とタスク一覧。
各フェーズは「完了条件」を満たしてから次へ進む。機能 ID（F-xx）は SPEC.md 第2部に対応する。

## 技術スタック（選定済み）

| 領域 | 採用 | 選定理由 |
|------|------|----------|
| フレームワーク | Next.js（App Router）+ TypeScript | レスポンシブ Web・サーバー側 API プロキシ（TMDb キー秘匿）・Vercel 無料枠との親和性 |
| ホスティング | Vercel（Hobby） | 無料枠制約（SPEC 第1部5章） |
| 認証・DB | Supabase（Auth + PostgreSQL + RLS） | 無料枠でマルチユーザー認証と行レベルセキュリティを賄える |
| スタイリング | Tailwind CSS | スマホ/PC 両対応の実装効率 |
| 作品情報 | TMDb API（language=ja-JP） | SPEC 第2部4章 |
| エラー監視 | Sentry（無料枠）【仮】 | フェーズ4で導入判断 |

## フェーズ 0: プロジェクトセットアップ

**完了条件**: ローカルで空のアプリが起動し、CI が回り、Supabase に接続できる。

- [ ] Next.js + TypeScript + Tailwind のプロジェクト初期化
- [ ] ESLint / Prettier / TypeScript strict 設定
- [ ] GitHub リポジトリ整備（develop ブランチ作成、main/develop の運用開始）
- [ ] GitHub Actions で lint / type-check / test の CI
- [ ] Supabase プロジェクト作成、ローカル開発環境（supabase cli）接続
- [ ] TMDb API キー取得、環境変数管理（.env.local / Vercel 環境変数）
- [ ] Vercel 連携（プレビューデプロイ確認）

## フェーズ 1: 認証基盤（F-01, F-02）

**完了条件**: 登録 → メール確認 → ログイン → ログアウト → 退会 が一巡でき、他ユーザーのデータに RLS でアクセスできないことをテストで確認済み。

- [ ] DB スキーマ v1（users は Supabase Auth 管理、profiles テーブル）+ RLS ポリシー
- [ ] 会員登録・メール確認フロー（S-2）
- [ ] ログイン / ログアウト / パスワード再設定
- [ ] 未ログイン時のリダイレクト（ミドルウェア）
- [ ] ログイン試行制限（5回失敗で10分ロック【仮】）
- [ ] 退会（トランザクションでの全データ削除）
- [ ] RLS の越境アクセス拒否テスト（F-01 受け入れ基準2）
- [ ] 未決-6（登録済みメールの開示可否）を決定して反映

## フェーズ 2: 鑑賞記録のコア機能（F-03〜F-06）

**完了条件**: 検索 → 記録 → 一覧 → 詳細 → 編集 → 削除 が SPEC の受け入れ基準・例外系どおりに動く。スマホ幅（375px）で全画面が使える。

- [ ] デザイントークンの実装（DESIGN.md §2 を Tailwind theme 拡張として定義。色値のハードコード禁止）
- [ ] movies / viewing_records テーブル + ユニーク制約 + RLS
- [ ] TMDb 検索のサーバー側プロキシ（キー秘匿・タイムアウト3秒・エラー劣化）
- [ ] 作品検索画面（S-4）: 結果表示・0件案内・API 障害時メッセージ
- [ ] 記録作成フォーム（S-5）: 必須/任意バリデーション、未来日付拒否、重複時の編集誘導、失敗時の入力保持
- [ ] コレクション一覧（S-3）: ポスターグリッド・鑑賞日降順・ページング・空状態・プレースホルダー画像
- [ ] 記録詳細（S-6）・編集・削除（確認ダイアログ）
- [ ] 並び替え・鑑賞方法フィルタ（Should。時間があれば）
- [ ] 未決-2（評価スケール）を決定して反映

## フェーズ 3: アチーブメント（F-07, F-08）

**完了条件**: F-07 の受け入れ基準（10本目で解錠・削除しても剥奪されない）がテストで通り、定義の追加がデータ投入のみで行える。

- [ ] achievements / user_achievements テーブル + ユニーク制約
- [ ] 判定エンジン（記録の作成・更新をトリガーに未解錠分を判定。記録保存とは独立に失敗しうる設計）
- [ ] 判定種別の実装: 総本数 / ジャンル別本数 / ジャンル種類数 / 鑑賞方法別本数 / 期間内本数 / 公開年レンジ
- [ ] 未決-1: アチーブメント最終ラインナップの確定 → シードデータ投入
- [ ] 解錠演出（トースト等）
- [ ] アチーブメント一覧画面（S-7）: 解錠済み/未解錠の区別、進捗表示（Should）
- [ ] 二重解錠・剥奪なしのテスト

## フェーズ 4: リリース準備（F-09、非機能）

**完了条件**: 本番 URL で全機能が動作し、SPEC 第2部5章の非機能要件（仮値含む）を満たすことを確認済み。

- [ ] 利用規約・プライバシーポリシーのページ（S-9）
- [ ] TMDb 帰属表示（フッター）
- [ ] ランディングページ（S-1）
- [ ] 未決-3（退会猶予）・未決-4（バックアップ方式）を決定して実装（週1 pg_dump【仮】）
- [ ] エラー監視（Sentry 無料枠【仮】）導入
- [ ] 性能確認: 一覧2秒以内・保存1秒以内・検索3秒以内（仮値）を実測
- [ ] 対応ブラウザ・画面幅 375px〜1920px での表示確認
- [ ] 未決-7（サービス名・ドメイン）を決定して本番デプロイ

## フェーズ 5（将来・着手判断は成功指標の実測後）

初期リリースには含めない。SPEC「やらないことリスト」のフェーズ2候補。

- [ ] 統計ダッシュボード（月別本数・ジャンル分布）
- [ ] 観たいリスト（ウィッシュリスト）
- [ ] 同一映画の複数回鑑賞記録（データモデル変更を伴うため要設計）
- [ ] Google OAuth ログイン
- [ ] 作品情報の手動登録（TMDb 非掲載作品対策）
- [ ] 未決-8: 成功指標の見直し（リリース3ヶ月後）

## 運用ルール

- ブランチ: `feature/` `fix/` `chore/` プレフィックス。main / develop への直接コミット禁止、マージは PR 経由（マージ先は develop）
- コミット: Conventional Commits（日本語説明文）
- 各フェーズ完了時に SPEC.md の未決事項リストを見直し、決定済みの項目は本文へ反映して未決から削除する
