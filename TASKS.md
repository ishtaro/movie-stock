

# movie-stock 開発タスク・今後の方針

SPEC.md の要件を実現するためのフェーズ分割とタスク一覧。
各フェーズは「完了条件」を満たしてから次へ進む。機能 ID（F-xx）は SPEC.md 第2部に対応する。

## 技術スタック（選定済み）

| 領域 | 採用 | 選定理由 |
|------|------|----------|
| フレームワーク | Next.js（App Router）+ TypeScript | レスポンシブ Web・サーバー側 API プロキシ（TMDb キー秘匿）・Vercel 無料枠との親和性 |
| ホスティング | Vercel（Hobby） | 無料枠制約（SPEC 第1部5章） |
| 認証・DB | Supabase（Auth + PostgreSQL + RLS） | 無料枠でマルチユーザー認証と行レベルセキュリティを賄える |
| スタイリング | Tailwind CSS | スマホ/PC 両対応の実装効率 |
| 作品情報 | TMDb API（language=ja-JP） | SPEC 第2部4章 |
| エラー監視 | Sentry（無料枠）【仮】 | フェーズ4で導入判断 |

## フェーズ 0: プロジェクトセットアップ

**完了条件**: ローカルで空のアプリが起動し、CI が回り、Supabase に接続できる。

- [x] Next.js + TypeScript + Tailwind のプロジェクト初期化（Next.js 16 / React 19 / Tailwind v4）
- [x] ESLint / Prettier / TypeScript strict 設定
- [x] GitHub リポジトリ整備（main/develop 作成・デフォルト develop・両ブランチに PR 必須/強制 push 禁止/削除禁止のルールセット。2026-07-15）
- [x] GitHub Actions CI: check（lint/型/ユニット/ビルド）+ integration-e2e（Supabase ローカルスタックでの統合13件 + Playwright E2E 3本）の2ジョブ構成
- [x] ローカル開発環境（supabase cli + Docker）接続・マイグレーション適用（※本番用クラウドプロジェクトの作成は未。手順は README.md）
- [x] TMDb API キー取得、環境変数管理（.env.local 設定済み / Vercel 環境変数は未）
- [ ] Vercel 連携（プレビューデプロイ確認）

## フェーズ 1: 認証基盤（F-01, F-02）

**完了条件**: 登録 → メール確認 → ログイン → ログアウト → 退会 が一巡でき、他ユーザーのデータに RLS でアクセスできないことをテストで確認済み。

- [x] DB スキーマ v1（users は Supabase Auth 管理、profiles テーブル）+ RLS ポリシー
- [x] 会員登録・メール確認フロー（S-2）
- [x] ログイン / ログアウト / パスワード再設定
- [x] 未ログイン時のリダイレクト（Next.js 16 の proxy.ts）
- [x] ログイン試行制限: Supabase Auth 標準のレート制限（IP 単位）に委ねる方針で確定（SPEC F-01 反映済み。アカウント単位ロックはフェーズ5で再検討）
- [x] 退会（auth ユーザー削除 + FK cascade による原子的な全データ削除）
- [x] RLS の越境アクセス拒否テスト（F-01 受け入れ基準2）（tests/integration/ で実 DB に対して検証。`npm run test:integration`）
- [x] 未決-6: 開示しない汎用文言を採用（登録済みメールでも同じ成功メッセージ）

## フェーズ 2: 鑑賞記録のコア機能（F-03〜F-06）

**完了条件**: 検索 → 記録 → 一覧 → 詳細 → 編集 → 削除 が SPEC の受け入れ基準・例外系どおりに動く。スマホ幅（375px）で全画面が使える。

- [x] デザイントークンの実装（DESIGN.md §2 を Tailwind v4 の @theme として定義。色値のハードコード禁止）
- [x] movies / viewing_records テーブル + ユニーク制約 + RLS
- [x] TMDb 検索のサーバー側プロキシ（キー秘匿・タイムアウト3秒・エラー劣化）
- [x] 作品検索画面（S-4）: 結果表示・0件案内・API 障害時メッセージ
- [x] 記録作成フォーム（S-5）: 必須/任意バリデーション、未来日付拒否、重複時の編集誘導、失敗時の入力保持
- [x] コレクション一覧（S-3）: ポスターグリッド・鑑賞日降順・ページャ・空状態・プレースホルダー画像（無限スクロールではなくページャで実装【仮】→未決-D4）
- [x] 記録詳細（S-6）・編集・削除（確認ダイアログ）
- [ ] 並び替え・鑑賞方法フィルタ（Should）（※並び替えは実装済み。鑑賞方法フィルタが未）
- [x] 未決-2: 1〜5の整数（星タップ・再タップで解除）で実装

## フェーズ 3: アチーブメント（F-07, F-08）

**完了条件**: F-07 の受け入れ基準（10本目で解錠・削除しても剥奪されない）がテストで通り、定義の追加がデータ投入のみで行える。

- [x] achievements / user_achievements テーブル + ユニーク制約
- [x] 判定エンジン（記録の作成・更新をトリガーに未解錠分を判定。記録保存とは独立に失敗しうる設計）
- [x] 判定種別の実装: 総本数 / ジャンル別本数 / ジャンル種類数 / 鑑賞方法別本数 / 期間内本数 / 公開年レンジ
- [ ] 未決-1: アチーブメント最終ラインナップの確定（※叩き台13個をシード投入済み）
- [x] 解錠演出（トースト・順次表示・reduced-motion 対応）
- [x] アチーブメント一覧画面（S-7): 解錠済み/未解錠の区別、進捗表示
- [x] 二重解錠・剥奪なしのテスト（エンジン単体テスト + 実 DB 統合テストの両方で検証済み）

## フェーズ 4: リリース準備（F-09、非機能）

**完了条件**: 本番 URL で全機能が動作し、SPEC 第2部5章の非機能要件（仮値含む）を満たすことを確認済み。

- [x] 利用規約・プライバシーポリシーのページ（S-9）（※内容は【仮】ドラフト。リリース前に確定）
- [x] TMDb 帰属表示（フッター）: 公式ロゴ（blue short）+ 規定文言を全画面に常設
- [x] ランディングページ（S-1）
- [x] 未決-3（退会=即時削除）・未決-4（週次 pg_dump）を確定・実装（.github/workflows/backup.yml。要 Secret: SUPABASE_DB_URL）
- [ ] エラー監視（Sentry 無料枠【仮】）導入（※要 Sentry アカウント・DSN）
- [x] 性能確認: ローカル（一覧 0.15s / 検索 0.05s / 保存 0.23s）・本番（一覧 0.58s / 検索 0.62s / 保存 0.85s ウォーム・約1.9s コールド）とも基準内。※当初 Vercel 関数が米国リージョンで保存 7.6s → vercel.json で東京（hnd1）に固定して解消
- [x] 対応画面幅 375px〜1920px での表示確認（tests/e2e/responsive.spec.ts で全11画面 × 3幅の横スクロールなしを自動検証）
- [x] 本番デプロイ: https://movie-stock-phi.vercel.app （Supabase 東京 + Vercel hnd1。スモークテスト全項目パス。サービス名は「MOVIE STOCK」・独自ドメインは未決-7 のまま）
- [ ] カスタム SMTP の設定（**正式公開前に必須**: Supabase 内蔵 SMTP は2通/時のため、Resend 等の無料枠を接続する。未対応の間は確認メールが詰まりうる）

## 追加要望（リリース後の機能追加）

- [x] おすすめ作品セクション（F-11・2026-07-15 要望）: コレクション下部にランダムカテゴリの人気作品を最大10件表示。記録済み作品は除外、TMDb 障害時は非表示、Suspense ストリーミングで一覧表示を遅延させない。ユニットテスト + 実環境検証済み

## フェーズ 5（将来・着手判断は成功指標の実測後）

初期リリースには含めない。SPEC「やらないことリスト」のフェーズ2候補。

- [ ] 統計ダッシュボード（月別本数・ジャンル分布）
- [ ] 観たいリスト（ウィッシュリスト）
- [ ] 同一映画の複数回鑑賞記録（データモデル変更を伴うため要設計）
- [ ] Google OAuth ログイン
- [ ] 作品情報の手動登録（TMDb 非掲載作品対策）
- [ ] 未決-8: 成功指標の見直し（リリース3ヶ月後）

## 運用ルール

- ブランチ: `feature/` `fix/` `chore/` プレフィックス。main / develop への直接コミット禁止（ルールセットで強制）、マージは PR 経由（マージ先は develop）
- リリース: develop → main の PR。main へのマージで Vercel が本番へ自動デプロイ（Git 連携）
- コミット: Conventional Commits（日本語説明文）
- CI: 全 PR と main/develop への push で実行。check ジョブ（数分）+ integration-e2e ジョブ（Supabase スタック起動込み）
- 各フェーズ完了時に SPEC.md の未決事項リストを見直し、決定済みの項目は本文へ反映して未決から削除する
