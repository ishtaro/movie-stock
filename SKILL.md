---
name: fable-grade-testing
description: Webアプリ等の制作後・機能実装後にテストを作成するタスク（ユニットテスト・結合テスト・E2Eテストの新規作成、テスト不足箇所の補強、テスト戦略の設計）を開始する前に読み込むスキル。Claude Fable 5 がテスト対象の選定・ケース設計・テスト自体の検証に用いる規律を明文化したもので、Opus 4.8 など他のモデルでも同水準のテスト品質を再現するために適用する。既存テストの軽微な修正だけのタスクには不要。
---

# Fable-Grade Testing — 制作後のテスト作成プロトコル

テストの品質は「カバレッジの数字」ではなく、**壊れたときに落ちるテストを、壊れると被害が大きい場所に置けているか**で決まる。そして書いたテストは、**落ちることを確認して初めて**テストとして信頼できる。テストコードを書き始める前にこのプロトコルを通すこと。

## 0. 大原則

1. **テストは実装の写しではなく、仕様の表明。** 「コードがこう動く」をなぞるテストは、実装ごと間違っていても通る。「こう動くべき」を仕様・要件側から独立に書く。実装を見ながら期待値をコピーしない。
2. **通るテストではなく、壊れたら落ちるテストを書く。** テストの価値は通ることではなく、バグの混入時に落ちること。新しく書いたテストは、実装を意図的に壊して**落ちることを確認**するまで完成ではない。
3. **カバレッジは目的ではなく副産物。** 数字を上げるための自明なテスト（getter、定数、フレームワークの機能）は書かない。その保守コストは負債になる。
4. **テストは資産であると同時に負債。** 1本増えるごとに実行時間と保守コストが増える。少なく・強く・速くを目指し、同じ欠陥を検出できるなら、より下の層（ユニット）でテストする。

## 1. 着手前の確認

1. **既存のテスト基盤と慣習を確認する。** テストフレームワーク、ディレクトリ構成、命名規則、既存のヘルパー・ファクトリ・フィクスチャを調べ、それに合わせる。新しいテストライブラリを勝手に追加しない。
2. **テストの実行方法を先に確かめる。** 既存テストを1回実行し、全部通る状態から始める（最初から落ちているテストがあれば、報告してから進める。自分の変更と既存の失敗を混ぜない）。
3. **このテストが何を守るのかを1文で書く。** 「決済フローのリグレッション防止」「リファクタ前の安全網」「バグ #123 の再発防止」。守るものが言えないテストは書く必要があるか疑う。

## 2. どこに何を書くか — 層の選択と対象の優先順位

### 2-1. テストピラミッド

| 層 | 対象 | 量 |
|---|---|---|
| ユニット | 純粋なロジック（計算・変換・バリデーション・分岐） | 最多。速く、失敗位置が特定できる |
| 結合 | API エンドポイント、DB を含む処理、コンポーネント + 状態 | 中間。境界の噛み合わせを守る |
| E2E | ユーザーの重要フロー（登録→購入→完了） | 最少。クリティカルパスのみ |

- 判断基準: **その欠陥を検出できる最も下の層で書く**。E2E でしか検出できないもの（画面遷移の噛み合わせ、認証込みのフロー）だけを E2E に残す。
- ロジックが UI に埋まっていてユニットテストできない場合、テストのために抽出するのは正当なリファクタリング（ただし振る舞いを変えず、抽出後に結合層でも1本通す）。

### 2-2. 対象の優先順位

すべてはテストできない。以下の順で書く。

1. **事故時の被害が大きい箇所**: 決済・課金、認証・認可、データの削除・上書き、外部への送信（メール・通知）
2. **複雑な分岐・計算**: 条件が3つ以上絡む箇所、日付・金額・端数処理
3. **バグが出た箇所**: 修正と同時に再発防止テストを書く（同じ場所は再び壊れる）
4. **境界・契約**: API のリクエスト/レスポンス形式、他チーム・外部が依存するインターフェース

自明なもの（単純な委譲、フレームワーク自体の動作、静的な表示）はテストしない。

## 3. ケース設計 — 正常系1本で終わらせない

各テスト対象について、次を機械的に列挙してから書く。

### 3-1. 入力の軸

- **境界値**: 0、1、最大、最大+1、空文字、空配列（同値クラスの真ん中ではなく端を突く）
- **不正**: null / undefined、型違い、フォーマット違反、負数
- **日本語・実データ**: 全角、絵文字、サロゲートペア、極端に長い文字列、前後の空白
- **重複・再実行**: 同じ操作を2回（冪等性、二重送信）

### 3-2. 状態の軸

- 対象が存在しない・削除済み・アーカイブ済み
- 権限のないユーザー・未ログイン・他人のリソース ID（**認可のテストは Web アプリで最優先**——URL の ID を書き換えたら他人のデータが見えないか）
- 状態遷移の禁止経路（下書き→完了への直接遷移など、遷移してはいけない組を選んで確認）

### 3-3. 失敗の軸

- 依存先の失敗: 外部 API のエラー・タイムアウト、DB 制約違反
- 途中失敗: 一連の処理の途中で失敗したとき、中途半端な状態が残らないか（トランザクション）

全部を書く必要はない。**対象の性質から壊れやすい軸を選び、正常系1本 + 選んだ軸で最低2〜4本**が目安。軸を選ばず正常系だけ量産するのが最悪のパターン。

## 4. テストコードの規律

- **構成は Arrange-Act-Assert**（準備・実行・検証）を視覚的に分ける。1テスト1観点。検証が5個も10個も並ぶテストは分割する。
- **テスト名は仕様を語る**: 「割引率が上限を超える場合は上限値に丸められる」のように、落ちたときに名前だけで何が壊れたか分かる文にする。`test1` `正常系2` は禁止。
- **決定性を守る**: 現在時刻・乱数・実行順序・ネットワークに依存させない。時刻と乱数は注入またはモックで固定する。「たまに落ちる」テストは無いより悪い（全テストへの信頼を毀損する）。
- **テスト間を独立に**: 各テストが自分のデータを用意し、他のテストの実行結果・実行順に依存しない。共有のグローバル状態に書き込まない。
- **モックは境界だけ**: 外部 API・時刻・メール送信など「制御できない・副作用がある」境界をモックする。自分のコードの内部までモックすると、モック同士の会話をテストするだけで実物を何も検証しなくなる。DB は可能ならテスト用の実物（インメモリ・コンテナ）を使う。
- **テストデータは意図が見える最小限**: そのテストの検証に関係する値だけを明示し、残りはファクトリ・ヘルパーの既定値に任せる。関係ない項目が20個並ぶテストは、何を検証しているか読めない。
- **アサーションは具体的に**: 「エラーにならないこと」ではなく「この値がこうなること」。例外の検証は型・メッセージまで確認する。スナップショットテストは「差分が出たら無条件に更新」される運用に堕ちやすいので、意図の明確な明示的アサーションを優先する。

## 5. E2E テストの規律（Playwright 等）

- **クリティカルパスに絞る**: 「新規登録→主要機能を1回使う→結果が残る」のような、事業が止まるフローだけ。画面の網羅は E2E の仕事ではない。
- セレクタは role・ラベル・`data-testid` を使う。CSS クラスや DOM 構造への依存は、無関係なスタイル変更で壊れる。
- 待機は明示的な条件（要素の出現、レスポンス完了）で行い、固定 sleep を使わない（遅い環境で落ち、速い環境で無駄になる）。
- テストごとに独立したユーザー・データを作る（並列実行と再実行に耐える）。
- 認証はセッションの使い回し等で高速化してよいが、「ログイン自体のテスト」だけは実際のフローで1本残す。

## 6. テスト自体の検証 — 完了報告の前に必ず実施

テストを書き終えたことと、テストが機能することは別。

- [ ] **落ちることを確認した**: 実装の対象箇所を意図的に壊し（条件反転、値の変更）、書いたテストが落ちることを確認して戻した。壊しても落ちないテストは検証力ゼロ——アサーションかモックが間違っている
- [ ] 新規テストを含む**テストスイート全体**を実行し、全部通ることを確認した（自分のテストだけ実行して完了としない）
- [ ] 新規テストを**2〜3回繰り返し実行**し、不安定さ（flaky）がないことを確認した
- [ ] テストの実行時間が異常に長くなっていない（固定 sleep・不要な E2E 化を疑う）
- [ ] テストコードが既存の慣習（配置・命名・ヘルパー利用）に沿っている

## 7. 報告

- **何をカバーし、何をカバーしていないか**を明示する。「テストを追加しました」ではなく「認可とバリデーション境界を追加した。同時実行と外部 API 障害系は未カバー」まで書く。
- テスト実行結果は実際の出力に基づいて報告する。実行せずに「通るはず」と報告しない。
- 既存コードのバグをテスト作成中に見つけたら、テストで隠蔽（バグに合わせた期待値）せず、バグとして報告する。**現状の動作に合わせたテストを書いてよいのは、その動作が正しいと確認できたときだけ。**

## アンチパターン早見表

| アンチパターン | 代わりにやること |
|---|---|
| 実装の出力をそのまま期待値にコピー | 仕様から期待値を導く。導けないなら仕様を確認する |
| 書いたテストが全部一発で通って完了 | 実装を意図的に壊し、落ちることを確認してから完了とする |
| カバレッジ目標のために自明なコードをテスト | 被害の大きい箇所・複雑な分岐・バグ跡地に優先順位を置く |
| 正常系だけを機能の数だけ量産 | 対象ごとに壊れやすい軸（境界・権限・失敗系）を選んで突く |
| 内部実装までモックして単体テスト | モックは制御できない境界だけ。内部は実物を通す |
| `sleep(3)` で非同期を待つ | 要素・イベント・レスポンスの明示的な条件で待つ |
| 何でも E2E でテストする | 検出できる最も下の層に置き、E2E はクリティカルパスに絞る |
| 既存バグに合わせた期待値でテストを通す | バグとして報告し、正しい仕様のテストを別途用意する |
| 「テスト追加しました」とだけ報告 | カバーした軸と未カバーの軸を明示して報告する |
