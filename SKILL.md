---
name: fable-grade-webapp
description: Webアプリ・Webサイトの実装タスク（新規制作・機能追加・改修・フォーム実装・UI実装）を開始する前に読み込むスキル。Claude Fable 5 がWeb制作で適用するHTML/CSS/JS/バリデーション/レスポンシブ/アクセシビリティ/セキュリティ/検証の規律を明文化したもので、Opus 4.8 など他のモデルでも同水準の実装品質を再現するために適用する。Webに関係しないCLIやバックエンドのみの作業には不要。
---

# Fable-Grade Web App — Webアプリ制作プロトコル

Web アプリの品質は「動くコードが書けるか」ではなく、**Web 特有の制約（多様な画面幅・信頼できない入力・不安定なネットワーク・多様な利用者）をすべて想定して書けるか**で決まる。実装を始める前にこのプロトコルを通し、完了報告の前にセクション9の検証を必ず実施すること。

## 0. 着手前の確認

1. **既存スタックを尊重する**。使用中のフレームワーク・CSS手法・バリデーションライブラリを確認し、それに合わせる。新しいライブラリを勝手に追加しない（追加が必要なら理由を添えて提案する）。
2. **対象環境を確定する**。対応ブラウザ・最小画面幅（既定: 375px）・ダークモード対応の有無。指定がなければモダンブラウザ + モバイルファーストを既定とする。
3. **完了条件は「ブラウザで実際に動くこと」**。コードが書けた時点ではなく、実際に動かして観測した時点で完了とする（セクション9）。

## 1. HTML — 意味のある構造を書く

- 要素は見た目ではなく**意味**で選ぶ。ページ内動作は `<button>`、遷移は `<a href>`。div + onclick でボタンを作らない（キーボード操作・フォーカス・支援技術が全部壊れる）。
- ランドマーク（header / nav / main / footer）と正しい見出し階層（h1は1つ、レベルを飛ばさない）でページを構造化する。
- `<html lang="ja">`、`<meta charset="utf-8">`、`<meta name="viewport" content="width=device-width, initial-scale=1">` を必ず設定する。`user-scalable=no` は使わない（ズーム禁止はアクセシビリティ違反）。
- 画像には alt（装飾なら `alt=""`）、`width`/`height` 属性（CLS防止）を付ける。
- 一覧は ul/ol、表データは table、日付は `<time>`。意味のある要素があるのに div で組まない。
- 実装後、W3C Markup Validator 相当のチェック（重複ID・入れ子違反・必須属性）を通す。

## 2. CSS・レスポンシブ

### 2-1. モバイルファースト

- 基本スタイルをモバイル向けに書き、`min-width` のメディアクエリで広い画面に拡張する。
- ブレークポイントはデバイス名ではなく**レイアウトが破綻する幅**で決める。目安: 600px / 1024px。
- **375px〜1920px のどの幅でも横スクロールが発生しない**ことを完了条件に含める。

### 2-2. 壊れないCSSのルール

- 幅は固定 px でなく `%` / `max-width` / `min()` / `clamp()` で指定する。`width: 800px` ではなく `max-width: 800px; width: 100%`。
- 文字サイズは rem 基準。ユーザーのブラウザ設定拡大に追従させる。
- レイアウトは flexbox / grid。flex アイテムのはみ出しは `min-width: 0` の欠落を疑う。
- 高さを固定しない。コンテンツ量（長い日本語、翻訳、ユーザー入力）が増えても破綻しない組み方にする。
- 長い連続文字列（URL・メールアドレス）による突き抜けは `overflow-wrap: break-word` で防ぐ。
- タッチターゲットは 44×44px 以上。隣接するリンク・ボタンの間隔を確保する。
- ホバーでしか出せない UI を作らない（タッチデバイスにホバーはない）。`@media (hover: hover)` で分岐する。
- ダークモード対応時は色をカスタムプロパティで定義し、`prefers-color-scheme` で切り替える。片方のテーマでしか確認しないことを禁止。
- アニメーションには `prefers-reduced-motion: reduce` の無効化分岐を必ず入れる。

## 3. フォームとバリデーション

### 3-1. 二重バリデーションの原則

**クライアント側バリデーションは UX、サーバー側バリデーションがセキュリティ。**クライアント側は迂回可能（DevTools・直接POST）なので、サーバー側の検証を省略することは絶対にない。クライアント側だけ実装して完了としない。

### 3-2. クライアント側

- HTML 標準機能を先に使う: `required` / `type="email"` / `maxlength` / `min` / `max` / `pattern`。JS での再発明はその後。
- 入力支援属性を必ず設定する: `autocomplete`（name / email / tel / postal-code 等）、`inputmode`（numeric / tel / email）、スマホのキーボード種別が変わる。
- バリデーションのタイミング: 初回は blur 時（入力中に怒らない）、一度エラーになった欄は入力のたびに再判定して回復を即座に伝える。
- エラー表示の要件:
  - 該当フィールドの直近にインライン表示する（ページ上部にまとめて出すだけにしない）
  - 「入力が不正です」ではなく**何をどう直せばよいか**を書く（「メールアドレスの形式で入力してください（例: name@example.com）」）
  - 色だけで伝えない（アイコン・テキストを併用）
  - `aria-invalid="true"` と `aria-describedby` でエラーメッセージをフィールドに関連付ける
- 日本語入力の考慮: 全角数字・全角スペースは弾く前に正規化を検討する。IME 変換確定の Enter でフォームを送信させない（keydown の isComposing / keyCode 229 を確認）。
- 送信処理: 送信中はボタンを無効化 + ローディング表示（二重送信防止）。失敗時は入力値を保持したままエラーを表示する（入力を消し飛ばさない）。

### 3-3. サーバー側

- すべての入力を「攻撃者が細工できる値」として扱う。型・長さ・範囲・形式・許可リストで検証する。
- クライアントと同じルールをサーバーでも検証する。加えて、認可（その操作をそのユーザーがしてよいか）はサーバーでのみ判定する。
- バリデーションエラーは 4xx とフィールド単位のエラー情報で返し、フロントが表示に使える形にする。

## 4. JavaScript・状態管理

### 4-1. 非同期 UI の4状態

データを取得・送信する UI は、**ローディング / 成功 / エラー / 空**の4状態をすべて設計・実装する。成功パスだけ作って完了としない。

- ローディング: スピナーまたはスケルトン。レイアウトシフトを起こさない。
- エラー: ユーザーが次に何をすべきか（再試行ボタン等）を提示する。
- 空: 「データがありません」+ 最初の1件を作る導線。空配列で白画面にしない。

### 4-2. 壊れない JS のルール

- 連打・二重実行に耐える（送信中フラグ、AbortController で前のリクエストを中断）。
- 検索など連続入力に紐づくリクエストは debounce + 古いレスポンスの破棄（レースコンディションで古い結果が新しい結果を上書きする事故を防ぐ）。
- イベントリスナ・タイマー・監視は、要素やコンポーネントの破棄時に必ず解除する。
- JS エラーで白画面にしない。エラーバウンダリ（またはグローバルハンドラ）で最低限の表示を守る。
- 状態の単一情報源を守る。同じデータを複数の場所に持たない（表示のズレの温床）。
- `console.log`・デバッグコードを残したまま完了としない。

## 5. アクセシビリティ

- **キーボードだけで全機能が操作できる**こと。Tab で到達でき、Enter/Space で実行でき、Esc で閉じられる。
- フォーカス管理: モーダルを開いたらフォーカスを中に移し、閉じたら元の要素に戻す。フォーカスをモーダル内に閉じ込める。`outline` を消すなら代替のフォーカス表示を必ず用意する。
- テキストのコントラスト比 4.5:1 以上（大きな文字は 3:1）。プレースホルダを label の代わりにしない。
- ARIA は最後の手段。ネイティブ要素（button / details / dialog / select）で実現できるものに role を後付けしない。使う場合は状態（`aria-expanded` 等）の更新まで実装する。
- 動的な内容変更（検索結果件数、送信完了）はスクリーンリーダーに伝わるよう `aria-live` を検討する。

## 6. セキュリティ

- **XSS**: ユーザー由来の値を `innerHTML` / `dangerouslySetInnerHTML` / `v-html` に渡さない。フレームワークの自動エスケープを迂回しない。どうしても HTML を挿入する場合はサニタイザ（DOMPurify 等）を通す。URL を href に入れる場合は `javascript:` スキームを弾く。
- **認可**: UI で隠すことと認可は別物。ボタンを非表示にしても API は叩ける。すべての操作・リソース取得でサーバー側の権限チェックを行う（ID の推測で他人のリソースに触れないか = IDOR を確認）。
- **CSRF**: 状態変更を伴うリクエストには CSRF トークンまたは SameSite Cookie + カスタムヘッダ検証を行う。GET で状態を変更しない。
- **Cookie**: `Secure` / `HttpOnly` / `SameSite` を設定する。トークンを localStorage に置く設計は XSS 時の被害を広げることを認識して選ぶ。
- **秘密情報**: API キー・シークレットをクライアントコードに埋め込まない(ビルド後の JS は全世界に公開される)。クライアントに渡してよいのは公開可能なキーのみ。
- **出力にも注意**: エラーレスポンスにスタックトレース・内部パス・SQL を含めない。
- レスポンスヘッダ: `X-Content-Type-Options: nosniff`、CSP（最低限 `frame-ancestors`）、`Referrer-Policy` を設定する。

## 7. パフォーマンス

- Core Web Vitals 目標: LCP 2.5s以下 / INP 200ms以下 / CLS 0.1以下。
- 画像: AVIF/WebP + `srcset`、ファーストビュー外は `loading="lazy"`、LCP 画像は preload または `fetchpriority="high"`。すべての画像に寸法指定（CLS 防止）。
- 日本語 Web フォントはサブセット化 + `font-display: swap`。
- JS は必要なページでだけ読む（動的 import・コード分割）。依存を1つ足す前にバンドルサイズへの影響を確認する。
- 一覧 API はページネーション必須。無限に伸びるリストを一括取得しない。N+1 クエリを作らない。
- 計測なしに最適化しない。遅いという報告には、まずどこが遅いか（ネットワーク / レンダリング / スクリプト）を DevTools で特定してから手を打つ。

## 8. エラー処理と信頼性

- ユーザー向けエラーメッセージは「何が起きたか + 次に何をすべきか」。内部用語・ステータスコードをそのまま見せない。
- ネットワークは失敗する前提で書く。fetch には必ずエラーハンドリングを付け、`response.ok` を確認する（fetch は 4xx/5xx で reject しない）。
- 破壊的操作（削除・上書き）には確認ステップまたは取り消し手段を付ける。
- ブラウザの「戻る」で壊れないか意識する（フォーム再送、stale な状態）。

## 9. 検証 — 完了報告の前に必ず実施

コードが書けたことと動くことは別。以下を**実際に動かして観測**してから完了を報告する。

### 9-1. 正常系

- [ ] 変更したフローをブラウザ（または curl / スクリーンショット）で実際に操作した
- [ ] ブラウザのコンソールにエラー・警告が出ていない
- [ ] ネットワークタブで失敗リクエスト・意図しない多重リクエストがない

### 9-2. 壊す検証（最低限この4つ）

- [ ] **空**: 未入力送信、データ0件の表示
- [ ] **過剰**: 極端に長い文字列、巨大なデータ件数、絵文字・機種依存文字
- [ ] **連打**: 送信ボタン連打、高速なページ遷移
- [ ] **迂回**: クライアントバリデーションを飛ばした直接リクエスト（サーバーが弾くか）

### 9-3. レスポンシブ・表示

- [ ] 375px / 768px / 1280px の3幅で表示確認、横スクロールなし
- [ ] キーボードのみで一通り操作できる
- [ ] （対応時）ダークモードの両テーマで確認

検証をスキップした項目は、完了報告に「未検証」と明記する。「おそらく動きます」で完了を装わない。

## アンチパターン早見表

| アンチパターン | 代わりにやること |
|---|---|
| div + onclick でボタンを作る | `<button>` を使う。キーボードとフォーカスが無料で付いてくる |
| クライアント側バリデーションだけで完了 | サーバー側で同じ検証 + 認可チェックを実装する |
| 成功パスだけ実装して完了報告 | ローディング/エラー/空の3状態を実装し、壊す検証を通す |
| PC の Chrome だけで確認 | 375px 幅とキーボード操作を最低限確認する |
| ユーザー入力を innerHTML に渡す | textContent か自動エスケープを使い、迂回しない |
| 「入力が不正です」とだけ表示 | どの欄を、どう直せばよいかをフィールドの隣に表示する |
| 幅・高さを px で固定してデザイン再現 | max-width と相対単位で組み、コンテンツ増減に耐えさせる |
| ボタンを隠して権限制御したつもり | サーバー側で認可を判定する。UI の非表示は演出にすぎない |
| コードを書き終えた時点で「完了しました」 | 実際に動かし、観測してから完了を報告する |
